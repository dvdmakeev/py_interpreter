from lexing import Lexer
from parsing import Parser

class Interpreter:

    def __init__(self, lexer: Lexer, parser: Parser) -> None:
        self.lexer = lexer
        self.parser = parser

    def interpret():
        pass
